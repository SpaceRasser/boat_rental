# 🚤 Boat Rental System

Система аренды лодок с ролями пользователей (клиент, арендодатель, администратор).

## ✨ Особенности

- ✅ **Автоматические миграции** - таблицы создаются автоматически при первом подключении
- ✅ **Роли пользователей**: клиент, арендодатель, администратор
- ✅ **Форма регистрации** с выбором роли через радио-кнопки
- ✅ **Профили для каждой роли** с соответствующим функционалом
- ✅ **Docker поддержка** - готово к запуску из коробки

## 🚀 Быстрый старт

### Вариант 1: Docker (Рекомендуется)

```bash
# 1. Запустить все сервисы
docker-compose up -d

# 2. Подождать 20-30 секунд пока MySQL запустится

# 3. Открыть в браузере:
#    - React: http://localhost:3000
#    - PHP API: http://localhost:8080/api/
#    - База данных: localhost:3306
```

### Вариант 2: Локально (XAMPP)

1. **Запустить XAMPP** (Apache + MySQL)

2. **Создать базу данных:**
   ```sql
   CREATE DATABASE boat_rental_system;
   ```

3. **Выполнить миграции** (опционально - таблицы создадутся автоматически):
   ```bash
   mysql -u root -p boat_rental_system < init.sql
   ```

4. **Запустить React:**
   ```bash
   cd react-admin
   npm install
   npm start
   ```

5. **Открыть:** http://localhost:3000

## 📋 Структура проекта

```
boat_rental/
├── api/                    # PHP Backend API
│   ├── config.php         # Конфигурация + автоматические миграции
│   ├── auth/              # Аутентификация
│   ├── users/             # Пользователи
│   ├── boats/             # Лодки/Услуги
│   ├── products/          # Товары
│   ├── bookings/          # Бронирования
│   ├── orders/            # Заказы
│   └── payments/          # Платежи
├── react-admin/           # React Frontend
│   └── src/
│       ├── components/
│       │   ├── Auth/      # Регистрация/Вход
│       │   └── Profile/   # Профили по ролям
│       └── config.js      # Конфигурация API URL
├── init.sql              # SQL миграции (для Docker)
├── docker-compose.yml    # Docker конфигурация
└── README.md            # Этот файл
```

## 🔐 Роли пользователей

### Клиент
- Просмотр услуг
- Бронирование
- Редактирование профиля

### Арендодатель
- Добавление услуг
- Просмотр расписания
- Редактирование профиля

### Администратор
- Просмотр статистики
- Управление всеми данными через админ-панель

## 🗄️ База данных

### Автоматические миграции

Таблицы создаются **автоматически** при первом подключении к API через функцию `ensureTablesExist()` в `api/config.php`.

### Таблицы:

- `users` - Пользователи (клиенты, арендодатели, админы)
- `owners` - Арендодатели (отдельная таблица)
- `boats` - Лодки/Услуги
- `products` - Товары
- `bookings` - Заказы/бронирования лодок
- `booking_items` - Товары в заказах
- `payments` - Платежи

## 🔧 Настройка

### Переменные окружения (Docker)

В `docker-compose.yml`:
- `DB_HOST=db`
- `DB_NAME=boat_rental_system`
- `DB_USER=boat_user`
- `DB_PASSWORD=boat_password`

### Локальная настройка

В `api/config.php` измените значения по умолчанию:
```php
$host = 'localhost';
$dbname = 'boat_rental_system';
$username = 'root';
$password = '';
```

## 📝 API Endpoints

### Аутентификация
- `POST /api/auth/login.php` - Вход
- `GET /api/auth/get_current.php?user_id={id}` - Текущий пользователь

### Пользователи
- `GET /api/users/get.php` - Список
- `POST /api/users/create.php` - Создать
- `POST /api/users/update_simple.php` - Обновить
- `DELETE /api/users/delete.php?id={id}` - Удалить

### Лодки
- `GET /api/boats/get.php` - Список
- `POST /api/boats/create.php` - Создать
- `POST /api/boats/update.php` - Обновить
- `DELETE /api/boats/delete.php?id={id}` - Удалить

И другие...

## 🐛 Решение проблем

### Таблицы не создаются

1. Проверьте подключение к БД:
   ```bash
   docker-compose logs db
   ```

2. Таблицы создаются автоматически при первом API запросе

3. Или выполните вручную:
   ```bash
   php check_tables.php
   ```

### Ошибка подключения к БД

1. Проверьте, что MySQL запущен
2. Проверьте переменные окружения
3. Для Docker: `docker-compose restart db`

### React не подключается к API

1. Проверьте `react-admin/src/config.js`
2. Убедитесь, что API доступен: http://localhost:8080/api/
3. Проверьте CORS настройки в `api/config.php`

## 📞 Поддержка

Если что-то не работает:
1. Проверьте логи: `docker-compose logs`
2. Убедитесь, что все контейнеры запущены: `docker-compose ps`
3. Пересоздайте базу: `docker-compose down -v && docker-compose up -d`

## 🎯 Что дальше?

После запуска:
1. Зарегистрируйтесь как клиент/арендодатель/админ
2. Войдите в систему
3. Используйте соответствующий функционал для вашей роли

---

**Важно:** Все таблицы создаются автоматически при первом запросе к API. Никаких ручных миграций не требуется!
