# Руководство по миграции на Laravel

## ✅ Что сделано

1. **Создана структура Laravel проекта** в папке `backend/`
2. **Миграции** для всех таблиц (users, owners, boats, products, boat_orders, bookings, payments)
3. **Модели Eloquent** со всеми связями
4. **API контроллеры** для всех endpoints
5. **Роуты** сохранены совместимыми с PHP версией (включая `.php` в путях)
6. **Docker конфигурация** обновлена для Laravel

## 📁 Структура

```
backend/
├── app/
│   ├── Http/Controllers/  # Контроллеры API
│   ├── Models/            # Eloquent модели
│   └── Exceptions/        # Обработка ошибок
├── database/migrations/   # Миграции БД
├── routes/api.php         # API роуты
├── config/                # Конфигурация
└── Dockerfile             # Docker образ
```

## 🚀 Запуск

### Вариант 1: Docker (рекомендуется)

```bash
docker-compose down
docker-compose up -d --build laravel
```

### Вариант 2: Локально

```bash
cd backend
composer install
cp .env.example .env
php artisan key:generate
php artisan migrate
php artisan serve
```

## 🔄 Миграция данных

Если у вас уже есть данные в БД, миграции Laravel могут конфликтовать. 

**Вариант A:** Использовать существующую БД (таблицы уже созданы)
- Пропустите `php artisan migrate`
- Laravel будет использовать существующие таблицы

**Вариант B:** Пересоздать БД
- Удалите старые таблицы
- Запустите миграции Laravel

## ⚙️ Конфигурация

Файл `.env` должен содержать:
```
DB_HOST=db
DB_DATABASE=boat_rental_system
DB_USERNAME=boat_user
DB_PASSWORD=boat_password
```

## 📝 API Совместимость

Все endpoints работают так же:
- `/api/auth/login.php`
- `/api/users/get.php`
- `/api/boats/get.php`
- И т.д.

Формат ответов идентичен PHP версии:
```json
{
  "success": true,
  "data": {...}
}
```

## ⚠️ Важные заметки

1. **Пароли:** Используется MD5 (для совместимости с текущей БД)
2. **Первичные ключи:** Используются `id_user`, `id_boat` и т.д.
3. **Время:** Форматируется как `H:i` (09:00) через аксессоры моделей
4. **Даты:** Форматируются как `d.m.Y H:i` для `created_at`

## 🐛 Решение проблем

### Ошибка миграций
Если таблицы уже существуют, можно пропустить миграции или использовать `php artisan migrate:status`

### CORS ошибки
Проверьте `config/cors.php` и middleware

### 404 на API routes
Убедитесь, что nginx/Apache правильно настроен для Laravel
