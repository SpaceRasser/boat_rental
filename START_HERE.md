# 🚀 ИНСТРУКЦИЯ ПО ЗАПУСКУ

## Шаг 1: Запуск проекта

### Если используете Docker:

```bash
# Остановить старые контейнеры (если есть)
docker-compose down -v

# Запустить все сервисы
docker-compose up -d

# Подождать 30 секунд пока MySQL запустится
# Проверить статус:
docker-compose ps
```

### Если используете XAMPP:

1. Запустите Apache и MySQL в XAMPP
2. Откройте http://localhost/phpmyadmin
3. Создайте базу данных `boat_rental_system`
4. Таблицы создадутся автоматически при первом запросе к API

## Шаг 2: Проверка работы

1. **Откройте React приложение:** http://localhost:3000
2. **Зарегистрируйтесь** - выберите роль через радио-кнопки
3. **Войдите в систему**
4. **Попробуйте добавить услугу** (если вы арендодатель)

## Шаг 3: Если что-то не работает

### Проверьте логи:

```bash
# Docker
docker-compose logs db
docker-compose logs php
docker-compose logs react

# Или все сразу
docker-compose logs -f
```

### Проверьте таблицы:

```bash
# Через Docker
docker-compose exec db mysql -u boat_user -pboat_password boat_rental_system -e "SHOW TABLES;"

# Или через PHP скрипт
php check_tables.php
```

### Пересоздайте базу:

```bash
docker-compose down -v
docker-compose up -d db
# Подождите 30 секунд
docker-compose up -d
```

## ✅ Что должно работать:

- ✅ Регистрация с выбором роли
- ✅ Вход в систему
- ✅ Профили для разных ролей
- ✅ Добавление услуг (арендодатель)
- ✅ Просмотр услуг (клиент)
- ✅ Бронирование (клиент)
- ✅ Статистика (админ)

## 🎯 Важно:

**Таблицы создаются автоматически!** При первом запросе к API все таблицы будут созданы автоматически через функцию `ensureTablesExist()` в `api/config.php`.

Не нужно запускать миграции вручную - все работает из коробки!
